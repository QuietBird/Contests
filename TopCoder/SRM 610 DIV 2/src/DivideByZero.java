import java.util.*;
import java.math.*;
import static java.lang.Math.*;

public class DivideByZero {
	
	public int CountNumbers(int[] numbers) {
		Set<Integer> alreadyInList = new TreeSet<Integer>();
        for(int i = 0; i < numbers.length; i++)
            alreadyInList.add(numbers[i]);
        Arrays.sort(numbers);
        boolean newNumberAdded = false;

        return 0;
	}
}
